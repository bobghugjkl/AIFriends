# 第20次提交：实现更新虚拟角色前端页面

**提交哈希**：`e276237`  
**提交时间**：2026-02-05 21:01:33  
**提交者**：xuecan yan

---

## 一、这次提交做了什么

把 `UpdateCharacter.vue` 从空壳变成完整功能——从路由参数获取角色 ID，调用 `get_single` API 加载角色数据，填充到表单中，修改后提交更新。

同时顺便把导航栏"创作"按钮指向了一个具体角色（暂时硬编码 `character_id=1`），说明还在测试阶段。

---

## 二、使用了什么技术

| 技术/工具/库 | 用途 |
|-------------|------|
| onMounted 生命周期钩子 | 页面挂载后调用 GET 请求从后端加载角色数据，用于预填充表单 |
| route.params.character_id | 从动态路由 URL（/create/character/update/:id/）中提取角色 ID，作为 API 参数和后端查询依据 |
| Vue watch 响应式监听 | 子组件通过 watch 监听 props 变化，当父组件传入的角色数据就绪后自动同步到本地 ref |
| v-if 条件渲染 | 在角色数据加载完成前不渲染表单组件，避免子组件收到空 props 导致报错或空白 UI |
| FormData 条件添加 | 只将修改过的图片文件添加到 FormData（通过 Base64 字符串对比），节省带宽和后端处理 |
| Vue Router 动态路由 (:character_id) | 路由路径中带冒号的动态参数段，匹配 URL 中的实际值并注入 route.params，支持 RESTful 资源定位 |
| Axios GET params 配置 | 通过 {params: {character_id: id}} 传递查询参数，Axios 自动序列化为 URL 查询字符串 ?character_id=xxx |
| 组件复用模式 | CreateCharacter 和 UpdateCharacter 共享 Photo/Name/Profile/BackgroundImage 子组件，通过 props 区分空初始值和已有数据 |
| Base64 字符串对比优化 | 通过 !== 对比当前编辑后的图片与原始图片的 Base64 data URL，仅当修改时才添入 FormData 上传 |

## 三、完整源代码

### 3.1 `UpdateCharacter.vue`——核心逻辑

**文件**：`frontend/src/views/create/character/UpdateCharacter.vue`（从空壳变为完整功能，完整内容）

```vue
<script setup>
import Photo from "@/views/create/character/components/Photo.vue";
import Name from "@/views/create/character/components/Name.vue";
import Profile from "@/views/create/character/components/Profile.vue";
import BackgroundImage from "@/views/create/character/components/BackgroundImage.vue";
import {onMounted, ref, useTemplateRef} from "vue";
import {base64ToFile} from "@/js/utils/base64_to_file.js";
import api from "@/js/http/api.js";
import {useRoute, useRouter} from "vue-router";
import {useUserStore} from "@/stores/user.js";

const user = useUserStore()
const router = useRouter()
const route = useRoute()
const characterId = route.params.character_id
const character = ref(null)

onMounted(async () => {
  try {
    const res = await api.get('/api/create/character/get_single/', {
      params: {
        character_id: characterId,
      }
    })
    const data = res.data
    if (data.result === 'success') {
      character.value = data.character
    }
  } catch (err) {
  }
})

const photoRef = useTemplateRef('photo-ref')
const nameRef = useTemplateRef('name-ref')
const profileRef = useTemplateRef('profile-ref')
const backgroundImageRef = useTemplateRef('background-image-ref')
const errorMessage = ref('')

async function handleUpdate() {
  const photo = photoRef.value.myPhoto
  const name = nameRef.value.myName?.trim()
  const profile = profileRef.value.myProfile?.trim()
  const backgroundImage = backgroundImageRef.value.myBackgroundImage

  errorMessage.value = ''
  if (!photo) {
    errorMessage.value = '头像不能为空'
  } else if (!name) {
    errorMessage.value = '名字不能为空'
  } else if (!profile) {
    errorMessage.value = '角色介绍不能为空'
  } else if (!backgroundImage) {
    errorMessage.value = '聊天背景不能为空'
  } else {
    const formData = new FormData()
    formData.append('character_id', characterId)
    formData.append('name', name)
    formData.append('profile', profile)

    if (photo !== character.value.photo) {
      formData.append('photo', base64ToFile(photo, 'photo.png'))
    }

    if (backgroundImage !== character.value.background_image) {
      formData.append('background_image', base64ToFile(backgroundImage, 'background_image.png'))
    }

    try {
      const res = await api.post('/api/create/character/update/', formData)
      const data = res.data
      if (data.result === 'success') {
        await router.push({
          name: 'user-space-index',
          params: {
            user_id: user.id,
          }
        })
      } else {
        errorMessage.value = data.result
      }
    } catch (err) {
    }
  }
}
</script>

<template>
  <div v-if="character" class="flex justify-center">
    <div class="card w-120 bg-base-200 shadow-sm mt-16">
      <div class="card-body">
        <h3 class="text-lg font-bold my-4">更新角色</h3>
        <Photo ref="photo-ref" :photo="character.photo" />
        <Name ref="name-ref" :name="character.name" />
        <Profile ref="profile-ref" :profile="character.profile" />
        <BackgroundImage ref="background-image-ref" :backgroundImage="character.background_image" />

        <p v-if="errorMessage" class="text-sm text-red-500">{{ errorMessage }}</p>

        <div class="flex justify-center">
          <button @click="handleUpdate" class="btn btn-neutral w-60 mt-2">更新</button>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>

</style>
```

**逐行讲解**：

- `route.params.character_id` —— 从动态路由 `/create/character/update/:character_id/` 中获取 ID
- `character = ref(null)` —— 保存从后端加载的角色数据。初始值为 `null`，用于 template 中的 `v-if`
- `onMounted(async () => {...})` —— 页面挂载后从后端加载角色数据
- `api.get('/api/create/character/get_single/', {params: {character_id: characterId}})` —— GET 请求的查询参数通过 `{params: {...}}` 传入。Axios 自动把它序列化为 `?character_id=xxx` 拼到 URL 后面

**`v-if` 控制渲染时机**：数据加载完成前不渲染表单（避免 props 为空传到子组件）。子组件通过 `watch` 监听 props 变化，`character` 从 `null` 变成对象后自动更新内部 `myPhoto`、`myName` 等值。

**更新提交逻辑**：对比图片 Base64，只有修改过的才上传。

### 3.2 路由注册

**文件**：`frontend/src/router/index.js`（新增路由，完整内容）

```javascript
import {createRouter, createWebHistory} from 'vue-router'
import HomepageIndex from "@/views/homepage/HomepageIndex.vue";
import LoginIndex from "@/views/user/account/LoginIndex.vue";
import RegisterIndex from "@/views/user/account/RegisterIndex.vue";
import SpaceIndex from "@/views/user/space/SpaceIndex.vue";
import ProfileIndex from "@/views/user/profile/ProfileIndex.vue";
import {useUserStore} from "@/stores/user.js";
import UpdateCharacter from "@/views/create/character/UpdateCharacter.vue";

const router = createRouter({
  history: createWebHistory(import.meta.env.BASE_URL),
  routes: [
    {
      path: '/',
      component: HomepageIndex,
      name: 'homepage-index',
    },
    // ... 其他路由
    {
      path: '/create/character/update/:character_id/',
      component: UpdateCharacter,
      name: 'update-character',
      meta: {
        needLogin: true,
      },
    },
    {
      path: '/404/',
      component: NotFoundIndex,
      name: 'not-found',
    },
    // ...
  ],
})
```

动态参数 `:character_id`，访问 `/create/character/update/5/` 就编辑 id=5 的角色。

### 3.3 `NavBar.vue` 改动——临时指向更新角色

**文件**：`frontend/src/components/navbar/NavBar.vue`（"创作"按钮临时指向 hardcoded 角色）

```diff
-          <RouterLink v-if="user.isLogin()" :to="{name: 'create-index'}" ...>
+          <RouterLink v-if="user.isLogin()" :to="{name: 'update-character', params: {character_id: 1}}" ...>
```

### 3.4 `BackgroundImage.vue` 小改动

**文件**：`frontend/src/views/create/character/components/BackgroundImage.vue`

```diff
-      <div v-else class="w-15 h-25 rounded-box bg-base-300"></div>
+      <div v-else class="w-15 h-25 rounded-box bg-base-200"></div>
```

占位背景颜色从 `base-300` 改为 `base-200`。

### 3.5 `CreateCharacter.vue` 和 `ProfileIndex.vue`——去掉 console.log

```diff
     } catch (err) {
-      console.log(err)
     }
```

去掉两个文件中的 `console.log(err)`，生产环境不输出调试信息。

---

## 四、总结

| 操作 | URL | 组件 |
|------|-----|------|
| 创建角色 | `/create/` | `CreateCharacter.vue` |
| 编辑角色 | `/create/character/update/:id/` | `UpdateCharacter.vue` |

更新和创建的**表单结构完全一样**，只是初始数据来源不同——创建是空的，更新是从 API 加载。子组件（Photo、Name、Profile、BackgroundImage）完全复用，只通过 props 区分是"空初始值"还是"已有数据"。
