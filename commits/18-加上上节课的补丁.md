# 第18次提交：加上上节课的补丁

**提交哈希**：`2f74c1e`  
**提交时间**：2026-02-05 20:04:19  
**提交者**：xuecan yan

---

## 一、这次提交做了什么

修复了两个 bug：
1. 删除角色接口忘了返回成功响应
2. 更新角色接口的 except 分支忘了 `return`

---

## 二、使用了什么技术

| 技术/工具/库 | 用途 |
|-------------|------|
| Django REST Framework Response | 视图函数必须返回 Response 对象，否则前端收不到响应导致请求挂起 |
| QuerySet.delete() | 数据库删除操作，返回受影响的行数；幂等设计——删除不存在的记录不会报错 |

## 三、完整源代码

### 3.1 bug 1：`remove.py` 缺少成功响应

**文件**：`backend/web/views/create/character/remove.py`

**修复前**（第17次提交的原始代码）：

```python
from rest_framework.views import APIView
from rest_framework.response import Response
from rest_framework.permissions import IsAuthenticated

from web.models.character import Character


class RemoveCharacterView(APIView):
    permission_classes = [IsAuthenticated]
    def post(self, request):
        try:
            character_id = request.data['character_id']
            Character.objects.filter(pk=character_id, author__user=request.user).delete()
        except:
            return Response({
                'result': '系统异常，请稍后重试'
            })
```

**修复后**（本次提交后的完整内容）：

```python
from rest_framework.views import APIView
from rest_framework.response import Response
from rest_framework.permissions import IsAuthenticated

from web.models.character import Character


class RemoveCharacterView(APIView):
    permission_classes = [IsAuthenticated]
    def post(self, request):
        try:
            character_id = request.data['character_id']
            Character.objects.filter(pk=character_id, author__user=request.user).delete()
            return Response({
                'result': 'success',
            })
        except:
            return Response({
                'result': '系统异常，请稍后重试'
            })
```

**修复的 diff**：

```diff
             Character.objects.filter(pk=character_id, author__user=request.user).delete()
+            return Response({
+                'result': 'success',
+            })
```

**问题**：删除成功后没有返回任何响应，Django 不会报错，但前端收到的响应体为空，无法判断删除是否成功。加上 `return Response({...})` 后前端用 `if (data.result === 'success')` 就能正常处理。

### 3.2 bug 2：`update.py` 缺少 `return`

**文件**：`backend/web/views/create/character/update.py`

**修复前**（第17次提交的原始代码，except 分支缺少 return）：

```python
from django.utils.timezone import now
from rest_framework.response import Response
from rest_framework.views import APIView
from rest_framework.permissions import IsAuthenticated

from web.models.character import Character
from web.views.utils.photo import remove_old_photo


class UpdateCharacterView(APIView):
    permission_classes = [IsAuthenticated]
    def post(self, request):
        try:
            character_id = request.data['character_id']
            character = Character.objects.get(id=character_id, author__user=request.user)
            name = request.data['name'].strip()
            profile = request.data['profile'].strip()[:100000]
            photo = request.FILES.get('photo', None)
            background_image = request.FILES.get('background_image', None)

            if not name:
                return Response({
                    'result': "名字不能为空"
                })
            if not profile:
                return Response({
                    'result': '角色介绍不能为空'
                })
            if photo:
                remove_old_photo(character.photo)
                character.photo = photo
            if background_image:
                remove_old_photo(character.background_image)
                character.background_image = background_image
            character.name = name
            character.profile = profile
            character.update_time = now()
            character.save()
            return Response({
                'result': 'success',
            })
        except:
            Response({
                'result': '系统异常，请稍后重试'
            })
```

**修复后的 except 分支**：

```diff
         except:
-            Response({
+            return Response({
                 'result': '系统异常，请稍后重试'
             })
```

**问题**：`except` 分支调用了 `Response(...)` 但没有 `return`。Python 函数没有 `return` 语句时默认返回 `None`，导致异常情况下前端收不到响应，请求会一直 hang 直到超时。这是个典型的漏写 `return` 的 bug。

---

## 四、总结

| 修复 | 影响 |
|------|------|
| remove 加 return Response | 前端能收到删除成功的确认 |
| update except 加 return | 异常时前端能收到错误信息而不是超时 |
