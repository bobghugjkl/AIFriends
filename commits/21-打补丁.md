# 第21次提交：打补丁

**提交哈希**：`faeb8a8`  
**提交时间**：2026-02-10 20:07:36  
**提交者**：xuecan yan

---

## 一、修复删除角色时不删文件的 bug

**文件**：`backend/web/views/create/character/remove.py`

**改之前**：
```python
Character.objects.filter(pk=character_id, author__user=request.user).delete()
```

**改之后**：
```python
character = Character.objects.get(pk=character_id, author__user=request.user)
remove_old_photo(character.photo)
remove_old_photo(character.background_image)
character.delete()
```

**问题**：之前用 `.filter().delete()` 直接删数据库记录，但**角色头像和背景图文件还留在磁盘上**。长期运行后 `media/character/photos/` 和 `media/character/background_images/` 会堆满废弃文件。

**改进**：先取到对象 → 删两个图片文件 → 再删数据库记录。`.get()` 找不到角色会抛异常，进入 `except` 返回"系统异常"，逻辑更严谨。

---

## 二、还原导航栏"创作"按钮

**文件**：`NavBar.vue`

```diff
-          <RouterLink :to="{name: 'update-character', params: {character_id: 1}}" ...>
+          <RouterLink :to="{name: 'create-index'}" ...>
```

上节课为了测试更新角色功能，把"创作"按钮临时指向了 `character_id=1`。现在测试完成，恢复为指向创建角色页面。

---

## 三、总结

| 改动 | 原因 |
|------|------|
| 删除角色前先删磁盘文件 | 防止废弃图片堆积占空间 |
| 创作按钮改回 create-index | 测试代码还原 |
