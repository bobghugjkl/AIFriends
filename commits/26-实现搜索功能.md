# 第26次提交：实现搜索功能

**提交哈希**：`b4e7271`  
**提交时间**：2026-02-10 21:48:56  
**提交者**：xuecan yan

---

## 一、这次提交做了什么

实现了角色搜索功能——用户可以在导航栏搜索框输入关键词，按角色名或角色介绍匹配。搜索过程不刷新页面，通过 Vue Router 的 query 参数传递搜索词，首页响应式监听并重置列表。

同时修正了分页参数：`items_count + 1` → `items_count + 20`（上上提交遗留的调试代码）。

---

## 二、使用了什么技术

| 技术/工具 | 用途 |
|-----------|------|
| Django `Q` 对象 | 构造 OR 查询：`Q(name__icontains=x) \| Q(profile__icontains=x)` |
| `icontains` 字段查找 | 不区分大小写的 LIKE 模糊匹配，对应 SQL 的 `LIKE '%keyword%'` |
| Vue Router query params | 通过 `route.query.q` 在 URL 中传递搜索关键词 |
| Vue `watch` | 监听 `route.query` 变化，自动重置列表并重新搜索 |

## 三、完整源代码

### 3.1 后端——搜索查询

**文件**：`backend/web/views/homepage/index.py`（完整内容，新增搜索逻辑）

```python
from django.db.models import Q
from rest_framework.views import APIView
from rest_framework.response import Response

from web.models.character import Character


class HomepageIndexView(APIView):
    def get(self, request):
        try:
            items_count = int(request.query_params.get('items_count'))
            search_query = request.query_params.get('search_query', '').strip()
            if search_query:
                queryset = Character.objects.filter(
                    Q(name__icontains=search_query) | Q(profile__icontains=search_query)
                )
            else:
                queryset = Character.objects.all()
            characters_raw = queryset.order_by('-id')[items_count: items_count + 20]
            characters = []
            for character in characters_raw:
                author = character.author
                characters.append({
                    'id': character.id,
                    'name': character.name,
                    'profile': character.profile,
                    'photo': character.photo.url,
                    'background_image': character.background_image.url,
                    'author': {
                        'user_id': author.user_id,
                        'username': author.user.username,
                        'photo': author.photo.url,
                    }
                })
            return Response({
                'result': 'success',
                'characters': characters
            })
        except:
            return Response({
                'result': '系统异常，请稍后重试'
            })
```

**逐行讲解**：

- `Q(name__icontains=search_query) | Q(profile__icontains=search_query)` —— Django ORM 的**Q 对象**，用于构造复杂的 OR 查询。普通 `.filter()` 只能做 AND，`Q` 能做 `|`（OR）和 `~`（NOT）
- `icontains` —— **i** = case **i**nsensitive（不区分大小写），**contains** = 包含。即**模糊匹配**——数据库层面的 `LIKE '%keyword%'` 操作
- `|` —— Python 的位或运算符，在 Q 对象中表示 OR。`name__icontains OR profile__icontains` ——名字或角色介绍中包含关键词
- `search_query = request.query_params.get('search_query', '')` —— 用 `.get()` 取可选参数，不传搜索词时走 else 分支返回全部
- **同时修正了分页**：`items_count + 1` → `items_count + 20`

### 3.2 导航栏——搜索表单

**文件**：`frontend/src/components/navbar/NavBar.vue`（完整内容，新增搜索功能）

```vue
<script setup>
import {ref, watch} from "vue";
import {useRoute, useRouter} from "vue-router";
// ... 其他 imports ...

const user = useUserStore()
const searchQuery = ref('')
const router = useRouter()
const route = useRoute()

watch(() => route.query.q, newQ => {
  searchQuery.value = newQ || ''
})

function handleSearch() {
  router.push({
    name: 'homepage-index',
    query: {
      q: searchQuery.value.trim(),
    }
  })
}
</script>

<template>
  <!-- ... -->
  <div class="navbar-center w-4/5 max-w-180 flex justify-center">
    <form @submit.prevent="handleSearch" class="join w-4/5 flex justify-center">
      <input v-model="searchQuery" class="input join-item rounded-l-full w-4/5" placeholder="搜索你感兴趣的内容" />
      <button class="btn join-item rounded-r-full gap-0">
        <SearchIcon />
        搜索
      </button>
    </form>
  </div>
  <!-- ... -->
</template>
```

- 搜索框改为 `<form @submit.prevent="handleSearch">` —— 按 Enter 或点搜索按钮 → 调用 `handleSearch()`
- `router.push({name: 'homepage-index', query: {q: '关键词'}})` —— 跳转到首页，URL 变成 `/?q=关键词`
- `watch(() => route.query.q, ...)` —— 监听 URL 中的 `?q=` 参数变化，同步到输入框

### 3.3 首页——响应搜索词变化

**文件**：`frontend/src/views/homepage/HomepageIndex.vue`（完整内容，新增搜索响应）

```vue
<script setup>
import {nextTick, onBeforeUnmount, onMounted, ref, useTemplateRef, watch} from "vue";
import api from "@/js/http/api.js";
import Character from "@/components/character/Character.vue";
import {useRoute} from "vue-router";

const characters = ref([])
const isLoading = ref(false)
const hasCharacters = ref(true)
const sentinelRef = useTemplateRef('sentinel-ref')
const route = useRoute()

function checkSentinelVisible() {
  if (!sentinelRef.value) return false

  const rect = sentinelRef.value.getBoundingClientRect()
  return rect.top < window.innerHeight && rect.bottom > 0
}

async function loadMore() {
  if (isLoading.value || !hasCharacters.value) return
  isLoading.value = true

  let newCharacters = []
  try {
    const res = await api.get('/api/homepage/index/', {
      params: {
        items_count: characters.value.length,
        search_query: route.query.q || '',
      }
    })
    const data = res.data
    if (data.result === 'success') {
      newCharacters = data.characters
    }
  } catch (err) {
  } finally {
    isLoading.value = false
    if (newCharacters.length === 0) {
      hasCharacters.value = false
    } else {
      characters.value.push(...newCharacters)
      await nextTick()

      if (checkSentinelVisible()) {
        await loadMore()
      }
    }
  }
}

let observer = null
onMounted(async () => {
  await loadMore()

  observer = new IntersectionObserver(
    entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          loadMore()
        }
      })
    },
    {root: null, rootMargin: '2px', threshold: 0}
  )

  observer.observe(sentinelRef.value)
})

function reset() {
  characters.value = []
  isLoading.value = false
  hasCharacters.value = true
  loadMore()
}

watch(() => route.query.q, newQ => {
  reset()
})

onBeforeUnmount(() => {
  observer?.disconnect()
})
</script>

<template>
  <div class="flex flex-col items-center mb-12">
    <div class="grid grid-cols-[repeat(auto-fill,minmax(240px,1fr))] gap-9 mt-12 justify-items-center w-full px-9">
      <Character
        v-for="character in characters"
        :key="character.id"
        :character="character"
      />
    </div>

    <div ref="sentinel-ref" class="h-2 mt-8"></div>
    <div v-if="isLoading" class="text-gray-500 mt-4">加载中...</div>
    <div v-else-if="!hasCharacters" class="text-gray-500 mt-4">没有更多角色了</div>
  </div>
</template>

<style scoped>

</style>
```

当 `?q=` 变化时，**清空角色列表** → 重置分页状态 → 重新加载第一页。

**完整搜索链路**：
1. 用户在导航栏搜索框输入关键词 → 点搜索
2. `handleSearch()` → `router.push({query: {q: '关键词'}})` → URL 变成 `/?q=关键词`
3. 首页 `watch(query.q)` 触发 → `reset()` → `loadMore()` → `api.get('/api/homepage/index/', {params: {search_query: '关键词'}})`
4. 后端 `Q(name__icontains='关键词') | Q(profile__icontains='关键词')` → 返回匹配结果
5. 首页渲染过滤后的角色卡片

**为什么用 URL query 而不是直接传参数**：把搜索词放在 URL 里，用户可以直接复制 URL 分享搜索结果。这也是 SPA 的一种"状态保持"手段。

---

## 四、总结

| 改动 | 层次 | 说明 |
|------|------|------|
| `Q` 对象模糊查询 | 后端 | `name__icontains` OR `profile__icontains` |
| 搜索表单 | 前端-导航栏 | `router.push` 传 `query: {q}` |
| `watch(query.q)` | 前端-首页 | 响应式监听 URL 变化，重置并重新加载 |
