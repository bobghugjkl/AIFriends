# 第16次提交：实现编辑资料页面前端

**提交哈希**：`d69c98e`  
**提交时间**：2026-02-03 21:18:09  
**提交者**：xuecan yan

---

## 一、这次提交做了什么

把编辑资料页面从空壳变成完整功能：头像裁剪上传（引入 Croppie 库）、用户名修改、简介修改。使用 Vue 的**子组件 + `defineExpose`** 模式让父组件能读取子组件的数据。

---

## 二、引入 Croppie——图片裁剪库

```json
// package.json
+    "croppie": "^2.6.5",
```

**Croppie** 是一个纯 JavaScript 图片裁剪库。用户上传一张图后，可以拖动、缩放、旋转来选择想要的区域，最后裁剪成特定尺寸（如正方形头像）。

---

## 三、`base64ToFile`——Base64 转文件工具

**文件**：`frontend/src/js/utils/base64_to_file.js`（新文件）

```javascript
export function base64ToFile(base64, filename) {
  const arr = base64.split(',')
  const mime = arr[0].match(/:(.*?);/)[1]
  const bstr = atob(arr[1])
  let n = bstr.length
  const u8arr = new Uint8Array(n)
  while (n--) u8arr[n] = bstr.charCodeAt(n)
  return new File([u8arr], filename, { type: mime })
}
```

**逐行讲解**：

- Croppie 裁剪后输出的是 **Base64 字符串**（如 `"data:image/png;base64,iVBOR..."`）。但后端 `request.FILES` 需要 File 对象。这个函数做格式转换

- `const arr = base64.split(',')` —— Base64 data URL 的格式是 `"data:image/png;base64,<编码数据>"`，按 `,` 切成两段：`arr[0]`=元数据，`arr[1]`=Base64 编码的图像数据

- `mime = arr[0].match(/:(.*?);/)[1]` —— 从 `"data:image/png"` 提取 MIME 类型 `"image/png"`。正则 `:(.*?);` 匹配冒号后、分号前的任意字符

- `atob(arr[1])` —— **a**scii **to** **b**inary。把 Base64 编码的字符串解码为原始二进制字符串

- `new Uint8Array(n)` —— 创建长度为 n 的无符号 8 位整数数组（每个元素 0-255，对应一个字节）

- `while (n--) u8arr[n] = bstr.charCodeAt(n)` —— 循环把二进制字符串的每个字符转为 Unicode 码点值（0-255），存入数组。`charCodeAt` 返回指定位置字符的 UTF-16 编码值

- `new File([u8arr], filename, {type: mime})` —— 用字节数组创建浏览器标准的 File 对象，可以放进 FormData 上传

---

## 四、`ProfileIndex.vue`——编辑资料主页面

```vue
<script setup>
import Photo from "@/views/user/profile/components/Photo.vue";
import Username from "@/views/user/profile/components/Username.vue";
import Profile from "@/views/user/profile/components/Profile.vue";
import {useUserStore} from "@/stores/user.js";
import {ref, useTemplateRef} from "vue";
import {base64ToFile} from "@/js/utils/base64_to_file.js";
import api from "@/js/http/api.js";

const user = useUserStore()
const photoRef = useTemplateRef('photo-ref')
const usernameRef = useTemplateRef('username-ref')
const profileRef = useTemplateRef('profile-ref')
const errorMessage = ref('')

async function handleUpdate() {
  const photo = photoRef.value.myPhoto
  const username = usernameRef.value.myUsername.trim()
  const profile = profileRef.value.myProfile.trim()
  // ... 验证和提交
}
</script>
```

**核心模式——`useTemplateRef` + `defineExpose`**：

1. 父组件用 `useTemplateRef('photo-ref')` 获取子组件实例
2. 子组件用 `defineExpose({myPhoto})` 暴露内部数据
3. 父组件用 `photoRef.value.myPhoto` 读取子组件的数据

**为什么不用 v-model**：v-model 适合简单值（如字符串）。这里有 3 个子组件，如果用 v-model，每个都需要 emit 事件，而用 ref + expose 一次就能拿到所有值。这是一种"命令式"的数据访问方式。

**提交逻辑**：

```javascript
const formData = new FormData()
formData.append('username', username)
formData.append('profile', profile)
if (photo !== user.photo) {
  formData.append('photo', base64ToFile(photo, 'photo.png'))
}
await api.post('/api/user/profile/update/', formData)
```

- `new FormData()` —— 浏览器原生 API，用于构造 `multipart/form-data` 格式的请求体。上传文件必须用这个，不能传 JSON
- `if (photo !== user.photo)` —— 用户没改头像就不上传，节省带宽和后端处理
- `base64ToFile(photo, 'photo.png')` —— Base64 → File 对象 → FormData → 发给后端

---

## 五、`Photo.vue`——头像裁剪组件（最复杂的子组件）

```javascript
import Croppie from 'croppie'
import 'croppie/croppie.css'

const props = defineProps(['photo'])
const myPhoto = ref(props.photo)

watch(() => props.photo, newVal => {
  myPhoto.value = newVal
})
```

- `defineProps(['photo'])` —— 接收父组件传来的当前头像 URL（props）
- `watch(() => props.photo, newVal => { myPhoto.value = newVal })` —— 监听 props 变化。父组件的 `user.photo` 更新后，子组件自动同步。`watch` 的第一个参数是一个 getter 函数 `() => props.photo`，Vue 追踪这个函数的依赖

**Croppie 初始化**：

```javascript
let croppie = null

async function openModal(photo) {
  modalRef.value.showModal()      // 打开 DaisyUI modal
  await nextTick()                // 等 DOM 渲染完成
  if (!croppie) {
    croppie = new Croppie(croppieRef.value, {
      viewport: {width: 200, height: 200, type: 'square'},
      boundary: {width: 300, height: 300},
      enableOrientation: true,   // 支持旋转
      enforceBoundary: true,     // 裁剪区域不能超出边界
    })
  }
  croppie.bind({url: photo})    // 绑定图片到裁剪器
}
```

- `await nextTick()` —— Vue 异步更新 DOM，`showModal()` 后 DOM 还没完全渲染，`croppieRef.value` 可能是 `null`。`nextTick` 等 DOM 更新完成后才继续
- `new Croppie(element, options)` —— 在指定 DOM 元素上创建裁剪器
- `viewport: {width: 200, height: 200, type: 'square'}` —— 裁剪窗口是 200×200 的正方形
- `croppie.bind({url: photo})` —— 将图片加载到裁剪器。URL 可以是远程地址或 Base64

**裁剪和结果**：

```javascript
async function crop() {
  myPhoto.value = await croppie.result({
    type: 'base64',
    size: 'viewport',     // 输出和裁剪窗口一样大的图片
  })
  modalRef.value.close()
}
```

**文件选择**：

```javascript
function onFileChange(e) {
  const file = e.target.files[0]
  e.target.value = ''                 // 清空，允许选同一张图
  const reader = new FileReader()
  reader.onload = () => openModal(reader.result)
  reader.readAsDataURL(file)          // File → Base64 data URL
}
```

- `FileReader.readAsDataURL(file)` —— 浏览器原生 API，把用户选的图片文件转成 Base64 data URL
- `e.target.value = ''` —— 重置 `<input type="file">` 的值。如果不重置，用户再次选择同一张图时不会触发 `change` 事件（因为值没变）

**生命周期清理**：

```javascript
onBeforeUnmount(() => {
  croppie?.destroy()    // 可选链调用，croppie 为 null 时不报错
})
```

**暴露给父组件**：

```javascript
defineExpose({myPhoto})
```

---

## 六、`Username.vue` 和 `Profile.vue`——简单子组件

两个组件的结构相同：

```javascript
const props = defineProps(['username'])  // 或 ['profile']
const myUsername = ref(props.username)   // 用 props 初始值创建本地副本

watch(() => props.username, newVal => {  // props 变时同步
  myUsername.value = newVal
})

defineExpose({myUsername})  // 或 {myProfile}
```

**为什么要做"本地副本"而不直接用 props**：Vue 的 props 是**单向数据流**——子组件不应该直接修改 props。但输入框需要可编辑，所以创建一个 `ref` 作为 props 的副本，子组件修改副本，父组件通过 `defineExpose` 读取最终值。

---

## 七、总结

| 组件/文件 | 职责 |
|-----------|------|
| `ProfileIndex.vue` | 主页面，收集 3 个子组件的数据，发 FormData 到后端 |
| `Photo.vue` | 头像展示 + 点击换图 + Croppie 裁剪 + Base64 输出 |
| `Username.vue` | 用户名输入框 |
| `Profile.vue` | 简介多行文本框 |
| `base64_to_file.js` | Base64 → File 格式转换 |
| `CameraIcon.vue` | 相机图标（覆盖在头像上） |
