# 第35次提交：实现聊天记录的流式加载

**提交哈希**：`612b831`  
**提交时间**：2026-03-03 21:57:41  
**提交者**：xuecan yan

---

## 一、这次提交做了什么

1. AI 回复完成后**自动保存消息到数据库**（完整输入+输出+token 统计）
2. 对话界面**显示聊天气泡**（用户靠右绿色，AI 靠左灰色），流式打字效果
3. **向上滚动加载历史消息**（游标分页 + 滚动位置保持）

---

## 二、使用了什么技术

| 技术 | 用途 |
|------|------|
| `Message.objects.create()` | 流结束后保存聊天记录 |
| `json.dumps` + `model_dump()` | 将 LangChain 消息对象序列化 |
| 游标分页 `pk__lt` | 加载更早的历史消息 |
| DaisyUI `chat-start/chat-end` | 气泡组件 |
| `IntersectionObserver` | 哨兵触发加载历史（什么是IntersectionObserver？浏览器提供的一种高效监听"元素是否进入屏幕可视区"的API。在这个项目中，聊天记录列表顶部放了一个高度2px的隐藏"哨兵"div（`sentinelRef`），IntersectionObserver盯着它——当用户向上滚动到顶部、哨兵div出现在可视区时，自动触发`loadMore()`加载更早的历史消息。传统做法是用`scroll`事件+手动计算位置，但scroll事件每秒触发几十次，性能差；IntersectionObserver是浏览器底层优化的，不卡顿。就像自动门上的红外感应器——你走到了门口（哨兵出现在视野中），门自动打开（加载更多），不需要你手动按按钮（不需要监听滚动事件）） |
| `scrollHeight/scrollTop` 保持 | 加载历史后不跳位置 |

---

## 三、整体架构

```
发消息 → pushBackMessage(用户气泡) + pushBackMessage(空AI气泡)
       → streamApi → onmessage: addToLastMessage(token) 逐字填充AI气泡
       → 流结束 → Message.objects.create() 存数据库

向上滚动 → 哨兵进入视口 → loadMore() → GET /get_history/?last_message_id=X
         → pk__lt 游标分页 → pushFrontMessage 插到数组前 → 保持滚动位置
```

---

## 四、源码改动

### 4.1 `chat.py`——消息持久化

```diff
+            full_output = ''
             ...
                 if msg.content:
+                    full_output += msg.content
                     yield ...
-            print(full_usage)
+            input_tokens = full_usage.get('input_tokens', 0)
+            output_tokens = full_usage.get('output_tokens', 0)
+            total_tokens = full_usage.get('total_tokens', 0)
+            Message.objects.create(
+                friend=friend,
+                user_message=message[:500],
+                input=json.dumps(
+                    [m.model_dump() for m in inputs['messages']],
+                    ensure_ascii=False,
+                )[:10000],
+                output=full_output[:500],
+                input_tokens=input_tokens,
+                output_tokens=output_tokens,
+                total_tokens=total_tokens,
+            )
```

- `full_output += msg.content` —— 累积完整 AI 回复
- `full_usage.get('input_tokens', 0)` —— 安全取 token 统计
- `Message.objects.create(...)` —— 一次插入一行数据库记录
- `json.dumps([m.model_dump() for m in inputs['messages']], ensure_ascii=False)` —— 把发给 AI 的完整输入序列化为 JSON 字符串，供调试和分析
- `[:500]` / `[:10000]` —— 截断保护，防止超长内容

---

### 4.2 `get_history.py`——历史消息查询（新文件，32行）

```python
from rest_framework.response import Response
from rest_framework.views import APIView
from rest_framework.permissions import IsAuthenticated
from web.models.friend import Message


class GetHistoryView(APIView):
    permission_classes = [IsAuthenticated]
    def get(self, request):
        try:
            last_message_id = int(request.query_params.get('last_message_id'))
            friend_id = request.query_params.get('friend_id')
            queryset = Message.objects.filter(friend_id=friend_id, friend__me__user=request.user)
            if last_message_id > 0:
                queryset = queryset.filter(pk__lt=last_message_id)
            messages_raw = queryset.order_by('-id')[:10]
            messages = []
            for m in messages_raw:
                messages.append({
                    'id': m.id,
                    'user_message': m.user_message,
                    'output': m.output,
                })
            return Response({
                'result': 'success',
                'messages': messages,
            })
        except:
            return Response({
                'result': '系统异常，请稍后重试'
            })
```

**逐行讲解**：

- `last_message_id` —— 游标。0=首次加载，>0=加载比此 id 更早的消息
- `friend__me__user=request.user` —— 权限隔离
- `pk__lt=last_message_id` —— id 小于游标 = 更早的消息
- `.order_by('-id')[:10]` —— 最新 10 条（倒序）

---

### 4.3 `ChatHistory.vue`——消息列表 + 历史加载（新文件，130行）

```vue
<script setup>
import Message from "@/components/character/chat_field/chat_history/message/Message.vue";
import {nextTick, onBeforeUnmount, onMounted, useTemplateRef} from "vue";
import api from "@/js/http/api.js";

const props = defineProps(['history', 'friendId', 'character'])
const emit = defineEmits(['pushFrontMessage'])
const scrollRef = useTemplateRef('scroll-ref')
const sentinelRef = useTemplateRef('sentinel-ref')
let isLoading = false
let hasMessages = true
let lastMessageId = 0

function checkSentinelVisible() {
  if (!sentinelRef.value) return false
  const sentinelRect = sentinelRef.value.getBoundingClientRect()
  const scrollRect = scrollRef.value.getBoundingClientRect()
  return sentinelRect.top < scrollRect.bottom && sentinelRect.bottom > scrollRect.top
}

async function loadMore() {
  if (isLoading || !hasMessages) return
  isLoading = true
  let newMessages = []
  try {
    const res = await api.get('/api/friend/message/get_history/', {
      params: { last_message_id: lastMessageId, friend_id: props.friendId }
    })
    const data = res.data
    if (data.result === 'success') { newMessages = data.messages }
  } catch (err) {
  } finally {
    isLoading = false
    if (newMessages.length === 0) {
      hasMessages = false
    } else {
      const oldHeight = scrollRef.value.scrollHeight
      const oldTop = scrollRef.value.scrollTop
      for (const m of newMessages) {
        emit('pushFrontMessage', {
          role: 'ai', content: m.output, id: crypto.randomUUID(),
        })
        emit('pushFrontMessage', {
          role: 'user', content: m.user_message, id: crypto.randomUUID(),
        })
        lastMessageId = m.id
      }
      await nextTick()
      const newHeight = scrollRef.value.scrollHeight
      scrollRef.value.scrollTop = oldTop + newHeight - oldHeight
      if (checkSentinelVisible()) { await loadMore() }
    }
  }
}

let observer = null
onMounted(async () => {
  await loadMore()
  observer = new IntersectionObserver(
    entries => { entries.forEach(entry => { if (entry.isIntersecting) { loadMore() } }) },
    {root: null, rootMargin: '2px', threshold: 0}
  )
  observer.observe(sentinelRef.value)
})
onBeforeUnmount(() => { observer?.disconnect() })

async function scrollToBottom() {
  await nextTick()
  scrollRef.value.scrollTop = scrollRef.value.scrollHeight
}
defineExpose({ scrollToBottom })
</script>

<template>
  <div ref="scroll-ref" class="absolute top-18 left-0 w-90 h-112 overflow-y-scroll no-scrollbar">
    <div ref="sentinel-ref" class="h-2"></div>
    <Message v-for="message in history" :key="message.id"
      :message="message" :character="character" />
  </div>
</template>

<style scoped>
.no-scrollbar::-webkit-scrollbar { display: none; }
.no-scrollbar {
  -ms-overflow-style: none;
  scrollbar-width: none;
}
</style>
```

**核心逻辑：加载历史保持滚动位置**

```javascript
const oldHeight = scrollRef.value.scrollHeight   // 加载前总高度
const oldTop = scrollRef.value.scrollTop          // 加载前滚动位置
// ... 加载新消息，DOM 变高 ...
const newHeight = scrollRef.value.scrollHeight    // 加载后总高度（变高了）
scrollRef.value.scrollTop = oldTop + newHeight - oldHeight  // 保持用户看到的内容不跳
```

如果没有这四行，历史消息插入顶部后，用户当前看的内容会被推下去，体验很差。

---

### 4.4 `Message.vue`——聊天气泡（新文件，32行）

```vue
<script setup>
import {useUserStore} from "@/stores/user.js";
defineProps(['message', 'character'])
const user = useUserStore()
</script>

<template>
  <div v-if="message.content">
    <div v-if="message.role === 'ai'" class="chat chat-start">
      <div class="chat-image avatar">
        <div class="w-10 rounded-full">
          <img :src="character.photo" alt="">
        </div>
      </div>
      <div class="chat-bubble whitespace-pre-wrap">{{ message.content }}</div>
    </div>
    <div v-else class="chat chat-end">
      <div class="chat-image avatar">
        <div class="w-10 rounded-full">
          <img :src="user.photo" alt="">
        </div>
      </div>
      <div class="chat-bubble chat-bubble-success whitespace-pre-wrap">{{ message.content }}</div>
    </div>
  </div>
</template>
```

- `v-if="message.content"` —— 空内容不渲染（流式回复中占位气泡初始 content=''）
- `chat chat-start` —— DaisyUI 靠左气泡（AI）
- `chat chat-end` + `chat-bubble-success` —— DaisyUI 靠右绿色气泡（用户）
- `whitespace-pre-wrap` —— 保留换行符

---

### 4.5 `InputField.vue`——即时气泡 + 流式填充

```diff
+const emit = defineEmits(['pushBackMessage', 'addToLastMessage'])

   message.value = ''
+  emit('pushBackMessage', {role: 'user', content: content, id: crypto.randomUUID()})
+  emit('pushBackMessage', {role: 'ai', content: '', id: crypto.randomUUID()})
```

发送后立即创建两个气泡——用户气泡有内容，AI 气泡为空占位。

```diff
-          console.log(data.content)
+          emit('addToLastMessage', data.content)
```

每个 token 追加到 AI 占位气泡 → 逐字出现效果。

### 4.6 `ChatField.vue`——消息数组管理

```javascript
const history = ref([])

function handlePushBackMessage(msg) {
  history.value.push(msg)
  chatHistoryRef.value.scrollToBottom()
}
function handleAddToLastMessage(delta) {
  history.value.at(-1).content += delta
  chatHistoryRef.value.scrollToBottom()
}
function handlePushFrontMessage(msg) {
  history.value.unshift(msg)
}
```

- `at(-1)` —— 取数组最后一个元素（ES2022 语法）
- `push` —— 追加到末尾（新消息）
- `unshift` —— 插入到开头（历史消息）

---

## 五、总结

| 文件 | 操作 | 说明 |
|------|------|------|
| `chat.py` | +14/-1 | full_output 累积 + Message.objects.create |
| `get_history.py` | 新 32 行 | 游标分页历史查询 |
| `ChatHistory.vue` | 新 130 行 | 消息列表 + 滚动加载 + 位置保持 |
| `Message.vue` | 新 32 行 | DaisyUI 气泡组件 |
| `InputField.vue` | +5/-3 | emit 气泡创建和流式填充 |
| `ChatField.vue` | +12 | history 数组管理 |
