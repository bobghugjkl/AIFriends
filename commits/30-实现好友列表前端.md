# 第30次提交：实现好友列表前端

**提交哈希**：`1bbdd90`  
**提交时间**：2026-02-12 21:04:29  
**提交者**：xuecan yan

---

## 一、这次提交做了什么

把好友页（FriendIndex）从空壳变成完整的好友列表，和首页/个人空间一样的无限滚动模式。同时给 Character 卡片增加了"删除好友"按钮和事件冒泡修复。

---

## 二、使用了什么技术

| 技术/工具 | 用途 |
|-----------|------|
| Vue `emit` 模式 | 子组件通过 `emit('remove', friendId)` 通知父组件删除好友 |
| `@click.stop` 事件修饰符 | 阻止点击删除按钮时事件冒泡到外层聊天框打开逻辑 |
| 无限滚动模式 | 复用已有的 IntersectionObserver 哨兵机制 |

## 三、整体架构 / 数据流

```
好友列表页 (FriendIndex) 架构:

FriendIndex mount
     ↓
loadMore() → GET /api/friend/get_list/?items_count=N
     ↓
返回 data.friends (每条含 friend.character 嵌套角色数据)
     ↓
渲染 Character 卡片:
  canRemoveFriend=true
  friendId=friend.id
     ↓
用户点击删除按钮
     ↓
handleRemoveFriend() → POST /api/friend/remove/
     ↓
emit('remove', friendId)
     ↓
父组件: friends.value = friends.value.filter(f => f.id !== friendId)

三种场景下的 Character 卡片 props:
┌────────────┬───────────┬──────────────────┐
│   页面      │ canEdit   │ canRemoveFriend  │
├────────────┼───────────┼──────────────────┤
│ 首页       │ false     │ false            │
│ 个人空间   │ true      │ false            │
│ 好友列表   │ false     │ true             │
└────────────┴───────────┴──────────────────┘
```

---

## 四、新增文件：`FriendIndex.vue` —— 好友列表页

**路径**：`frontend/src/views/friend/FriendIndex.vue`

**完整源码（96 行）：**

```vue
<script setup>
import {nextTick, onBeforeUnmount, onMounted, ref, useTemplateRef} from "vue";
import Character from "@/components/character/Character.vue";
import api from "@/js/http/api.js";

const friends = ref([])
const isLoading = ref(false)
const hasFriends = ref(true)
const sentinelRef = useTemplateRef('sentinel-ref')

function checkSentinelVisible() {
  if (!sentinelRef.value) return false

  const rect = sentinelRef.value.getBoundingClientRect()
  return rect.top < window.innerHeight && rect.bottom > 0
}

async function loadMore() {
  if (isLoading.value || !hasFriends.value) return
  isLoading.value = true

  let newFriends = []
  try {
    const res = await api.get('/api/friend/get_list/', {
      params: {
        items_count: friends.value.length,
      }
    })
    const data = res.data
    if (data.result === 'success') {
      newFriends = data.friends
    }
  } catch (err) {
  } finally {
    isLoading.value = false
    if (newFriends.length === 0) {
      hasFriends.value = false
    } else {
      friends.value.push(...newFriends)
      await nextTick()

      if (checkSentinelVisible()) {
        await loadMore()
      }
    }
  }
}

let observer = null
onMounted(async () => {
  await loadMore()

  observer = new IntersectionObserver(
    entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          loadMore()
        }
      })
    },
    {root: null, rootMargin: '2px', threshold: 0}
  )

  observer.observe(sentinelRef.value)
})

function removeFriend(friendId) {
  friends.value = friends.value.filter(f => f.id !== friendId)
}

onBeforeUnmount(() => {
  observer?.disconnect()
})
</script>

<template>
  <div class="flex flex-col items-center mb-12">
    <div class="grid grid-cols-[repeat(auto-fill,minmax(240px,1fr))] gap-9 mt-12 justify-items-center w-full px-9">
      <Character
        v-for="friend in friends"
        :key="friend.id"
        :character="friend.character"
        :canRemoveFriend="true"
        :friendId="friend.id"
        @remove="removeFriend"
      />
    </div>

    <div ref="sentinel-ref" class="h-2 mt-8"></div>
    <div v-if="isLoading" class="text-gray-500 mt-4">加载中...</div>
    <div v-else-if="!hasFriends" class="text-gray-500 mt-4">没有更多聊天了</div>
  </div>
</template>

<style scoped>

</style>
```

**讲解：**
- 和 `HomepageIndex.vue` 几乎一样的无限滚动逻辑，区别：
  - API 调用 `/api/friend/get_list/`
  - 卡片传 `canRemoveFriend="true"` + `friendId="..."` 让用户能删除好友
  - 数据取 `data.friends`（好友列表），每条是 `friend.character`（嵌套的角色数据）
  - `removeFriend(friendId)` 从本地列表中过滤掉被删除的好友

---

## 五、修改文件：`Character.vue` —— 角色卡片新增删除好友功能

**路径**：`frontend/src/components/character/Character.vue`

**本次提交对 Character.vue 的完整 diff：**

```diff
-const props = defineProps(['character', 'canEdit'])
+const props = defineProps(['character', 'canEdit', 'canRemoveFriend', 'friendId'])
+const emit = defineEmits(['remove'])

 ...

+async function handleRemoveFriend() {
+  try {
+    const res = await api.post('/api/friend/remove/', {
+      friend_id: props.friendId,
+    })
+    if (res.data.result === 'success') {
+      emit('remove', props.friendId)
+    }
+  } catch (err) {
+  }
+}

 ...

   <div class="avatar cursor-pointer"
        @mouseover="isHover=true" @mouseout="isHover=false" @click="openChatField">

 ...

-    <RouterLink :to="{name: 'update-character', ...}" class="...">
+    <RouterLink @click.stop :to="{name: 'update-character', ...}" class="...">
       <UpdateIcon />
     </RouterLink>
-    <button @click="handleRemoveCharacter" class="...">
+    <button @click.stop="handleRemoveCharacter" class="...">
       <RemoveIcon />
     </button>

 ...

+    <div v-if="canRemoveFriend" class="absolute right-0 top-50">
+      <button @click.stop="handleRemoveFriend" class="btn btn-circle btn-ghost bg-transparent">
+        <RemoveIcon />
+      </button>
+    </div>
```

**新增逻辑说明：**

### 5.1 删除好友按钮

```html
<div v-if="canRemoveFriend" class="absolute right-0 top-50">
  <button @click.stop="handleRemoveFriend" class="btn btn-circle btn-ghost">
    <RemoveIcon />
  </button>
</div>
```

**新增 Props**：
- `canRemoveFriend` —— 是否显示删除好友按钮
- `friendId` —— 好友关系 ID，用于删除 API

**`handleRemoveFriend()` 函数：**
- 调用 `POST /api/friend/remove/` API 删除好友关系
- 成功后 `emit('remove', props.friendId)` 通知父组件从列表中移除

### 5.2 事件冒泡修复

```html
<button @click.stop="handleRemoveCharacter">
```

- 加 `.stop` 修饰符 —— `event.stopPropagation()`，阻止事件冒泡到外层 div
- 外层 div 的 `@click="openChatField"` 负责打开聊天框
- 如果点删除按钮时事件冒泡到外层，会变成**先删角色再弹出聊天框**的 bug
- `.stop` 阻止冒泡，点击删除只触发删除逻辑

同样 `@click.stop` 也加到了编辑链接（`RouterLink`）上，保证点击编辑按钮时不会同时打开聊天框。

---

## 六、总结

| 页面 | API | canEdit | canRemoveFriend |
|------|-----|---------|-----------------|
| 首页 | `homepage/index` | false | false |
| 个人空间 | `character/get_list` | true | false |
| 好友列表 | `friend/get_list` | false | true |

Character 卡片通过 3 种 props 控制 3 种场景下的按钮显示。
