# 第23次提交：实现角色卡片

**提交哈希**：`c532cdd`  
**提交时间**：2026-02-10 21:10:14  
**提交者**：xuecan yan

---

## 一、这次提交做了什么

实现了角色卡片组件 `Character.vue`——展示角色的背景图、头像、名字、简介，以及编辑/删除按钮。同时新增两个图标组件，把 SpaceIndex 的骨架填上了真正的卡片。

---

## 二、使用了什么技术

| 技术/工具 | 用途 |
|-----------|------|
| Vue `emit` / `defineEmits` | 子组件向父组件通信，如删除角色时触发 `remove` 事件 |
| DaisyUI card layout | 基于 Tailwind 的 UI 组件库的卡片样式 |
| CSS `transition` + `scale-120` | 鼠标悬停时背景图放大 1.2 倍的动画效果 |
| Tailwind `ring-3` | 头像外圈的白色环状边框 |
| Grid layout | 卡片网格布局，自动适应列数 |

## 三、完整源代码

### 3.1 `Character.vue`——角色卡片组件

**文件**：`frontend/src/components/character/Character.vue`（从空壳变为完整功能，完整内容）

```vue
<script setup>
import {ref} from "vue";
import {useUserStore} from "@/stores/user.js";
import UpdateIcon from "@/components/character/icons/UpdateIcon.vue";
import RemoveIcon from "@/components/character/icons/RemoveIcon.vue";
import api from "@/js/http/api.js";

const props = defineProps(['character', 'canEdit'])
const emit = defineEmits(['remove'])
const isHover = ref(false)
const user = useUserStore()

async function handleRemoveCharacter() {
  try {
    const res = await api.post('/api/create/character/remove/', {
      character_id: props.character.id,
    })
    if (res.data.result === 'success') {
      emit('remove', props.character.id)
    }
  } catch (err) {
  }
}
</script>

<template>
  <div>
    <div class="avatar cursor-pointer" @mouseover="isHover=true" @mouseout="isHover=false">
      <div class="w-60 h-100 rounded-2xl relative">
        <img :src="character.background_image" class="transition-transform duration-300" :class="{'scale-120': isHover}" alt="">
        <div class="absolute left-0 top-50 w-60 h-50 bg-linear-to-t from-black/40 to-transparent"></div>

        <div v-if="canEdit && character.author.user_id === user.id" class="absolute right-0 top-50">
          <RouterLink :to="{name: 'update-character', params: {character_id: character.id}}" class="btn btn-circle btn-ghost bg-transparent">
            <UpdateIcon />
          </RouterLink>
          <button @click="handleRemoveCharacter" class="btn btn-circle btn-ghost bg-transparent">
            <RemoveIcon />
          </button>
        </div>

        <div class="absolute left-4 top-54 avatar">
          <div class="w-16 rounded-full ring-3 ring-white">
            <img :src="character.photo" alt="">
          </div>
        </div>
        <div class="absolute left-24 right-4 top-58 text-white font-bold line-clamp-1 break-all">
          {{ character.name }}
        </div>
        <div class="absolute left-4 right-4 top-72 text-white line-clamp-4 break-all">
          {{ character.profile }}
        </div>
      </div>
    </div>
    <RouterLink :to="{name: 'user-space-index', params: {user_id: character.author.user_id}}" class="flex items-center mt-4 gap-2 w-60">
      <div class="avatar">
        <div class="w-7 rounded-full">
          <img :src="character.author.photo" alt="">
        </div>
      </div>
      <div class="text-sm line-clamp-1 break-all">{{ character.author.username }}</div>
    </RouterLink>
  </div>
</template>

<style scoped>

</style>
```

- `props.character` —— 角色数据对象（id、name、photo、background_image、profile、author）
- `props.canEdit` —— 布尔值，控制是否显示编辑/删除按钮。在个人主页（自己的空间）传 true，首页传 false（只能看别人的）
- `emit = defineEmits(['remove'])` —— 声明"我会向父组件发出 `remove` 事件"
- 删除成功后通过 `emit('remove', id)` 通知父组件

**卡片视觉效果**（从上到下）：
- 背景图（宽240）—— 悬停时 scale-120 放大，`transition-transform duration-300` 动画
- 底部渐变遮罩 `bg-linear-to-t from-black/40 to-transparent`
- 编辑/删除按钮——条件渲染 `v-if="canEdit && character.author.user_id === user.id"`
- 头像——白色 ring-3 边框，悬在渐变线上
- 角色名、角色简介——文字在渐变遮罩上
- 作者信息行——卡片下方独立区域，点击进入作者空间

### 3.2 `UpdateIcon.vue`——编辑图标

**文件**：`frontend/src/components/character/icons/UpdateIcon.vue`（新文件，完整内容）

```vue
<script setup>

</script>

<template>
  <svg
    xmlns="http://www.w3.org/2000/svg"
    class="w-4 h-4 text-white"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
  >
    <path d="M12 20h9" />
    <path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" />
  </svg>
</template>

<style scoped>

</style>
```

铅笔编辑图标 SVG。

### 3.3 `RemoveIcon.vue`——删除图标

**文件**：`frontend/src/components/character/icons/RemoveIcon.vue`（新文件，完整内容）

```vue
<script setup>

</script>

<template>
  <svg
    xmlns="http://www.w3.org/2000/svg"
    class="w-4 h-4 text-white"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
  >
    <path d="M3 6h18" />
    <path d="M8 6V4h8v2" />
    <path d="M6 6l1 14h10l1-14" />
    <path d="M10 11v6" />
    <path d="M14 11v6" />
  </svg>
</template>

<style scoped>

</style>
```

垃圾桶删除图标 SVG。

### 3.4 `SpaceIndex.vue`——集成角色卡片

**文件**：`frontend/src/views/user/space/SpaceIndex.vue`（新增角色卡片渲染，diff 内容）

```diff
+ import Character from "@/components/character/Character.vue";

// ... 在 template 中的 grid div 里：
+      <Character
+        v-for="character in characters"
+        :key="character.id"
+        :character="character"
+        :canEdit="true"
+        @remove="removeCharacter"
+      />

// 新增 removeCharacter 函数：
+ function removeCharacter(characterId) {
+   characters.value = characters.value.filter(c => c.id !== characterId)
+ }
```

**"哨兵"去掉红色背景**：

```diff
-    <div ref="sentinel-ref" class="h-2 mt-8 w-100 bg-red-500"></div>
+    <div ref="sentinel-ref" class="h-2 mt-8"></div>
```

上一版的红色是调试用的，现在功能验证通过就去掉了。

**去掉 console.log**：

```diff
     } catch (err) {
-      console.log(err)
     }
```

### 3.5 `UserInfoField.vue`——修复空值显示

**文件**：`frontend/src/views/user/space/components/UserInfoField.vue`

```diff
-  <div class="flex justify-center mt-12 gap-8">
+  <div v-if="userProfile" class="flex justify-center mt-12 gap-8">
```

加 `v-if="userProfile"` 防止在数据未加载时显示空内容。

### 3.6 `get_list.py`——分页数量临时改为 1（调试用）

**文件**：`backend/web/views/create/character/get_list.py`

```diff
-            ).order_by('-id')[items_count: items_count + 20]
+            ).order_by('-id')[items_count: items_count + 1]
```

这是调试用的——每次只返回 1 条方便测试无限滚动是否触发。下一提交会改回 20。

---

## 四、总结

| 新增 | 说明 |
|------|------|
| `Character.vue` | 角色卡片，支持悬停动画、编辑/删除按钮、点击作者跳转空间 |
| `UpdateIcon.vue` | 铅笔编辑图标 |
| `RemoveIcon.vue` | 垃圾桶删除图标 |
